# v1.43 — Hypothesis State Synchronization

The persistent reasoning graph must reflect the latest planning hypothesis state. When the orchestrator receives an existing hypothesis ID again with a changed probability or prediction set, the canonical hypothesis node is updated rather than retaining stale planning state.

The synchronization remains a projection boundary: it does not fabricate evidence, execute observations, or convert a probability into a verified conclusion.